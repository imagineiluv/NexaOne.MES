<?xml version="1.0" encoding="utf-8"?>
<queries module="SYS">
  <!-- 화면정의 영속 + 진입 대상 — JSON은 SYS_SCREEN_DEFINITION, 채널/완전 경로는 SYS_SCREEN_TARGET이 소유한다.
       대상행이 없는 레거시 정의는 MES + /meta/{UI_ID}로 해석한다. MSSQL판과 동일 ID·동일 의미. -->
  <query id="SYS.GetScreenDefinition" access="public">
    <statement><![CDATA[
SELECT D.UI_ID, D.TITLE, D.DEFINITION_JSON,
       COALESCE(T.TARGET_CHANNEL, 'MES') AS TARGET_CHANNEL,
       COALESCE(T.ENTRY_PATH, '/meta/' || D.UI_ID) AS ENTRY_PATH
FROM SYS_SCREEN_DEFINITION D
LEFT JOIN SYS_SCREEN_TARGET T ON T.UI_ID = D.UI_ID
WHERE D.UI_ID = @uiId
]]></statement>
  </query>
  <query id="SYS.ListScreenDefinitions" requiredPermission="sys:read">
    <statement><![CDATA[
SELECT D.UI_ID, D.TITLE,
       COALESCE(T.TARGET_CHANNEL, 'MES') AS TARGET_CHANNEL,
       COALESCE(T.ENTRY_PATH, '/meta/' || D.UI_ID) AS ENTRY_PATH
FROM SYS_SCREEN_DEFINITION D
LEFT JOIN SYS_SCREEN_TARGET T ON T.UI_ID = D.UI_ID
WHERE NULLIF(TRIM(@targetChannel), '') IS NULL
   OR COALESCE(T.TARGET_CHANNEL, 'MES') = UPPER(TRIM(@targetChannel))
ORDER BY D.UI_ID
]]></statement>
  </query>
  <query id="SYS.UpsertScreenDefinition" kind="write" requiredPermission="sys:manage">
    <statement><![CDATA[
INSERT INTO SYS_SCREEN_DEFINITION (UI_ID, TITLE, DEFINITION_JSON, CREATED_BY, CREATED_AT, UPDATED_BY, UPDATED_AT)
VALUES (@uiId, @title, @definitionJson, @currentUser, @utcNow, @currentUser, @utcNow)
ON CONFLICT(UI_ID) DO UPDATE SET
    TITLE = excluded.TITLE, DEFINITION_JSON = excluded.DEFINITION_JSON,
    UPDATED_BY = excluded.UPDATED_BY, UPDATED_AT = excluded.UPDATED_AT;

WITH NORMALIZED AS (
    SELECT @uiId AS UI_ID,
           UPPER(COALESCE(NULLIF(TRIM(@targetChannel), ''), 'MES')) AS TARGET_CHANNEL,
           NULLIF(TRIM(@entryPath), '') AS REQUESTED_ENTRY_PATH
), RESOLVED AS (
    SELECT N.UI_ID, N.TARGET_CHANNEL, N.REQUESTED_ENTRY_PATH,
           CASE N.TARGET_CHANNEL
               WHEN 'MOBILE' THEN '/Mobile/' || N.UI_ID
               WHEN 'POP' THEN '/POP/' || N.UI_ID
               ELSE '/meta/' || N.UI_ID
           END AS EXPECTED_ENTRY_PATH
    FROM NORMALIZED N
)
INSERT INTO SYS_SCREEN_TARGET
    (UI_ID, TARGET_CHANNEL, ENTRY_PATH, CREATED_BY, CREATED_AT, UPDATED_BY, UPDATED_AT)
SELECT UI_ID, TARGET_CHANNEL,
       CASE
           WHEN REQUESTED_ENTRY_PATH IS NULL THEN EXPECTED_ENTRY_PATH
           WHEN REQUESTED_ENTRY_PATH = EXPECTED_ENTRY_PATH THEN REQUESTED_ENTRY_PATH
           ELSE NULL
       END,
       @currentUser, @utcNow, @currentUser, @utcNow
FROM RESOLVED
WHERE 1 = 1
ON CONFLICT(UI_ID) DO UPDATE SET
    TARGET_CHANNEL = excluded.TARGET_CHANNEL, ENTRY_PATH = excluded.ENTRY_PATH,
    UPDATED_BY = excluded.UPDATED_BY, UPDATED_AT = excluded.UPDATED_AT;
]]></statement>
  </query>

  <!-- Designer 코드 시드 가져오기 전용. 첫 INSERT가 충돌하면 changes()=0이므로 대상 행도 만들지 않는다.
       일반 Upsert와 달리 기존 정의/대상은 어떤 경우에도 갱신하지 않는 insert-only 경계다. -->
  <query id="SYS.ImportSeedScreenDefinition" kind="write" requiredPermission="sys:manage">
    <statement><![CDATA[
INSERT INTO SYS_SCREEN_DEFINITION
    (UI_ID, TITLE, DEFINITION_JSON, CREATED_BY, CREATED_AT, UPDATED_BY, UPDATED_AT)
SELECT @uiId, @title, @definitionJson, @currentUser, @utcNow, @currentUser, @utcNow
WHERE NOT EXISTS (SELECT 1 FROM SYS_SCREEN_DEFINITION WHERE UI_ID = @uiId)
ON CONFLICT(UI_ID) DO NOTHING;

INSERT INTO SYS_SCREEN_TARGET
    (UI_ID, TARGET_CHANNEL, ENTRY_PATH, CREATED_BY, CREATED_AT, UPDATED_BY, UPDATED_AT)
SELECT @uiId,
       UPPER(COALESCE(NULLIF(TRIM(@targetChannel), ''), 'MES')),
       CASE
           WHEN NULLIF(TRIM(@entryPath), '') IS NULL THEN
               CASE UPPER(COALESCE(NULLIF(TRIM(@targetChannel), ''), 'MES'))
                   WHEN 'MOBILE' THEN '/Mobile/' || @uiId
                   WHEN 'POP' THEN '/POP/' || @uiId
                   WHEN 'MES' THEN '/meta/' || @uiId
                   ELSE NULL
               END
           WHEN TRIM(@entryPath) =
               CASE UPPER(COALESCE(NULLIF(TRIM(@targetChannel), ''), 'MES'))
                   WHEN 'MOBILE' THEN '/Mobile/' || @uiId
                   WHEN 'POP' THEN '/POP/' || @uiId
                   WHEN 'MES' THEN '/meta/' || @uiId
                   ELSE NULL
               END THEN TRIM(@entryPath)
           ELSE NULL
       END,
       @currentUser, @utcNow, @currentUser, @utcNow
WHERE changes() = 1;
]]></statement>
  </query>

  <!-- ===== 비민감 마스터 조회(S7, read 무해 — kind 미표기). MSSQL판과 동일 ID·동일 의미(SQLite 방언: NOLOCK 없음). ===== -->
  <!-- 보안 가드(S7): 사용자 조회는 비민감 컬럼만 — PASSWORD_HASH/PASSWORD_STATE/FAIL_COUNT/LOCKED_UNTIL는
       절대 SELECT하지 않는다(자격증명/잠금은 격리 인증 경로 db/queries-auth가 소유). 역할/메뉴는 자격증명 무관. -->

  <query id="SYS.ListRoles" requiredPermission="sys:manage">
    <statement><![CDATA[
SELECT ROLE_ID, ROLE_NAME, DESCRIPTION, PERMISSIONS
FROM SYS_ROLE
WHERE IS_DELETED = 0
ORDER BY ROLE_ID
]]></statement>
  </query>

  <query id="SYS.GetRole" requiredPermission="sys:manage">
    <statement><![CDATA[
SELECT ROLE_ID, ROLE_NAME, DESCRIPTION, PERMISSIONS
FROM SYS_ROLE
WHERE ROLE_ID = @roleId AND IS_DELETED = 0
]]></statement>
  </query>

  <query id="SYS.ListMenus" requiredPermission="sys:read">
    <statement><![CDATA[
SELECT MENU_ID, MENU_NAME, PARENT_MENU_ID, DISPLAY_SEQUENCE, MENU_TYPE, PROGRAM_ID, IMAGE_ID, OPTIONS, UI_ID
FROM SYS_MENU
WHERE VALID_STATE = 'Valid'
ORDER BY PARENT_MENU_ID, DISPLAY_SEQUENCE, MENU_TYPE, MENU_ID
]]></statement>
  </query>

  <!-- 사용자 목록(활성·비삭제) — 비민감 컬럼만(PASSWORD_HASH/PASSWORD_STATE/FAIL_COUNT/LOCKED_UNTIL 제외). -->
  <query id="SYS.ListUsers" requiredPermission="sys:user.manage">
    <statement><![CDATA[
SELECT USER_ID, USER_NAME, EMAIL, ROLE_ID, LANGUAGE, IS_ACTIVE, LAST_LOGIN_AT
FROM SYS_USER
WHERE IS_DELETED = 0
  AND (@roleId IS NULL OR ROLE_ID = @roleId)
ORDER BY USER_ID
]]></statement>
  </query>

  <!-- ===== 계층 메뉴(SmartUX 셸 사이드바·메뉴 관리 CRUD). MSSQL판과 동일 ID·동일 @param·동일 메타(SQLite 방언: NOLOCK 없음, upsert via ON CONFLICT). ===== -->
  <query id="SYS.MenuTree" requiredPermission="sys:read">
    <statement><![CDATA[
SELECT MENU_ID, MENU_NAME, PARENT_MENU_ID, DISPLAY_SEQUENCE, MENU_TYPE, UI_ID
FROM SYS_MENU
WHERE VALID_STATE = 'Valid'
ORDER BY PARENT_MENU_ID, DISPLAY_SEQUENCE, MENU_ID
]]></statement>
  </query>

  <!-- ===== 메뉴-역할 매핑(SYSTEM_2_MENU_AUTH_MANAGEMENT 메뉴별 권한 관리). =====
       가시성 의미론(하위호환): 매핑이 하나도 없는 화면 메뉴 = 공개, 매핑이 있으면 해당 역할만.
       폴더는 항상 통과(빈 폴더 정리는 셸 클라이언트). 전권('*') 역할은 매핑과 무관하게 전체를 본다.
       MenuTreeForUser는 @currentUser가 필요해 호스트 GET /api/v1/sys/menu-tree(토큰 바인딩)가 정상
       경로다 — /query 직접 호출은 @currentUser 미주입(DBNull)이라 매핑된 메뉴가 모두 숨는다(안전 방향). -->
  <query id="SYS.MenuTreeForUser" access="public">
    <statement><![CDATA[
SELECT m.MENU_ID, m.MENU_NAME, m.PARENT_MENU_ID, m.DISPLAY_SEQUENCE, m.MENU_TYPE, m.UI_ID
FROM SYS_MENU m
WHERE m.VALID_STATE = 'Valid'
  AND (
        m.MENU_TYPE <> 'Screen'
        OR NOT EXISTS (SELECT 1 FROM SYS_MENU_ROLE r
                        WHERE r.MENU_ID = m.MENU_ID AND r.VALID_STATE = 'Valid')
        OR EXISTS (SELECT 1
                     FROM SYS_MENU_ROLE r
                     JOIN SYS_USER u ON u.USER_ID = @currentUser AND u.IS_DELETED = 0
                    WHERE r.MENU_ID = m.MENU_ID AND r.VALID_STATE = 'Valid'
                      AND r.ROLE_ID = u.ROLE_ID)
        OR EXISTS (SELECT 1
                     FROM SYS_USER u
                     JOIN SYS_ROLE ro ON ro.ROLE_ID = u.ROLE_ID AND ro.IS_DELETED = 0
                    WHERE u.USER_ID = @currentUser AND u.IS_DELETED = 0
                      AND ('|' || ro.PERMISSIONS || '|') LIKE '%|*|%')
      )
ORDER BY m.PARENT_MENU_ID, m.DISPLAY_SEQUENCE, m.MENU_ID
]]></statement>
  </query>

  <query id="SYS.MenuRoleList" requiredPermission="sys:manage">
    <statement><![CDATA[
SELECT r.MENU_ID, m.MENU_NAME, r.ROLE_ID, ro.ROLE_NAME, r.VALID_STATE
FROM SYS_MENU_ROLE r
LEFT JOIN SYS_MENU m ON m.MENU_ID = r.MENU_ID
LEFT JOIN SYS_ROLE ro ON ro.ROLE_ID = r.ROLE_ID
WHERE r.VALID_STATE = 'Valid'
ORDER BY r.MENU_ID, r.ROLE_ID
]]></statement>
  </query>

  <!-- SYS_MENU_ROLE에는 감사 컬럼이 없다(V031 — 리포 쓰기 경로 부재로 미보유). FK(SYS_MENU/SYS_ROLE)로
       미존재 메뉴/역할 매핑은 제약 위반으로 거부된다. 삭제는 물리 삭제 — 마지막 매핑 제거 = 공개 복귀. -->
  <query id="SYS.UpsertMenuRole" kind="write" requiredPermission="sys:manage">
    <statement><![CDATA[
INSERT INTO SYS_MENU_ROLE (MENU_ID, ROLE_ID, VALID_STATE)
VALUES (@menuId, @roleId, 'Valid')
ON CONFLICT(MENU_ID, ROLE_ID) DO UPDATE SET VALID_STATE = 'Valid'
]]></statement>
  </query>

  <query id="SYS.DeleteMenuRole" kind="write" requiredPermission="sys:manage">
    <statement><![CDATA[
DELETE FROM SYS_MENU_ROLE WHERE MENU_ID = @menuId AND ROLE_ID = @roleId
]]></statement>
  </query>

  <!-- ===== 다국어(P3-14 v1) — 공통 문구 리소스(V031 테이블·V070 EnUs 시드)와 사용자 언어 설정. =====
       UserLanguage/UpdateUserLanguage는 @currentUser 스코프 — 호스트 GET/PUT /api/v1/sys/language가 정상 경로. -->
  <query id="SYS.LanguageResources" access="public">
    <statement><![CDATA[
SELECT RESOURCE_KEY, VALUE
FROM SYS_MULTI_LANGUAGE_RESOURCE
WHERE LANGUAGE = @language
]]></statement>
  </query>

  <query id="SYS.UserLanguage" access="public">
    <statement><![CDATA[
SELECT LANGUAGE FROM SYS_USER WHERE USER_ID = @currentUser AND IS_DELETED = 0
]]></statement>
  </query>

  <query id="SYS.UpdateUserLanguage" kind="write" requiredPermission="sys:manage">
    <statement><![CDATA[
UPDATE SYS_USER SET LANGUAGE = @language, UPDATED_BY = @currentUser, UPDATED_AT = @utcNow
WHERE USER_ID = @currentUser AND IS_DELETED = 0
]]></statement>
  </query>

  <!-- ===== 사용자 메뉴 개인화(§20.12 즐겨찾기/최근 메뉴) — 전부 @currentUser 스코프. =====
       쓰기는 서버 주입 @currentUser만 사용하므로 직접 /command 호출로도 타인 행 조작이 불가하고,
       읽기는 /query 직접 호출 시 @currentUser 미주입(DBNull)이라 0행 — 본 쿼리들은 호스트
       SysPersonalizationController가 토큰 사용자로 바인딩해 실행하는 것이 정상 경로다. -->

  <query id="SYS.ListFavoriteMenus" access="public">
    <statement><![CDATA[
SELECT F.MENU_ID, M.MENU_NAME, M.PROGRAM_ID, M.IMAGE_ID, M.UI_ID, F.DISPLAY_SEQUENCE
FROM SYS_FAVORITE_MENU F
JOIN SYS_MENU M ON M.MENU_ID = F.MENU_ID AND M.VALID_STATE = 'Valid' AND M.MENU_TYPE = 'Screen'
WHERE F.USER_ID = @currentUser
ORDER BY F.DISPLAY_SEQUENCE, F.MENU_ID
]]></statement>
  </query>
  <!-- 존재하는 Screen 메뉴만 + 중복 멱등 + 말미 순번 부여를 한 문장으로 — 미존재/중복이면 0행(성공 취급). -->
  <query id="SYS.AddFavoriteMenu" kind="write" requiredPermission="sys:manage">
    <statement><![CDATA[
INSERT INTO SYS_FAVORITE_MENU (USER_ID, MENU_ID, DISPLAY_SEQUENCE, CREATED_AT)
SELECT @currentUser, M.MENU_ID,
       COALESCE((SELECT MAX(F.DISPLAY_SEQUENCE) FROM SYS_FAVORITE_MENU F WHERE F.USER_ID = @currentUser), 0) + 1,
       @utcNow
FROM SYS_MENU M
WHERE M.MENU_ID = @menuId AND M.VALID_STATE = 'Valid' AND M.MENU_TYPE = 'Screen'
  AND NOT EXISTS (SELECT 1 FROM SYS_FAVORITE_MENU F2 WHERE F2.USER_ID = @currentUser AND F2.MENU_ID = M.MENU_ID)
]]></statement>
  </query>
  <query id="SYS.RemoveFavoriteMenu" kind="write" requiredPermission="sys:manage">
    <statement><![CDATA[
DELETE FROM SYS_FAVORITE_MENU WHERE USER_ID = @currentUser AND MENU_ID = @menuId
]]></statement>
  </query>
  <query id="SYS.UpdateFavoriteMenuSequence" kind="write" requiredPermission="sys:manage">
    <statement><![CDATA[
UPDATE SYS_FAVORITE_MENU SET DISPLAY_SEQUENCE = @sequence WHERE USER_ID = @currentUser AND MENU_ID = @menuId
]]></statement>
  </query>
  <query id="SYS.ListRecentMenus" access="public">
    <statement><![CDATA[
SELECT R.MENU_ID, M.MENU_NAME, M.PROGRAM_ID, M.IMAGE_ID, M.UI_ID, R.LAST_USED_AT
FROM SYS_RECENT_MENU R
JOIN SYS_MENU M ON M.MENU_ID = R.MENU_ID AND M.VALID_STATE = 'Valid' AND M.MENU_TYPE = 'Screen'
WHERE R.USER_ID = @currentUser
ORDER BY R.LAST_USED_AT DESC
LIMIT 10
]]></statement>
  </query>
  <!-- 탐색 부수 기록 — 미존재/폴더 메뉴는 조용히 0행(§20.12), 재사용은 LAST_USED_AT 갱신(맨 앞 이동). -->
  <query id="SYS.RecordRecentMenu" kind="write" requiredPermission="sys:manage">
    <statement><![CDATA[
INSERT INTO SYS_RECENT_MENU (USER_ID, MENU_ID, LAST_USED_AT)
SELECT @currentUser, M.MENU_ID, @utcNow
FROM SYS_MENU M
WHERE M.MENU_ID = @menuId AND M.VALID_STATE = 'Valid' AND M.MENU_TYPE = 'Screen'
ON CONFLICT(USER_ID, MENU_ID) DO UPDATE SET LAST_USED_AT = excluded.LAST_USED_AT
]]></statement>
  </query>
  <!-- 메뉴 사용 통계(V086) — 전역 누적(트림 없음). 기록은 RecordRecentMenu와 동일 지점에서 호출. -->
  <query id="SYS.RecordMenuUsage" kind="write" requiredPermission="sys:manage">
    <statement><![CDATA[
INSERT INTO SYS_MENU_USAGE (MENU_ID, USE_COUNT, LAST_USED_AT)
SELECT M.MENU_ID, 1, @utcNow
FROM SYS_MENU M
WHERE M.MENU_ID = @menuId AND M.VALID_STATE = 'Valid' AND M.MENU_TYPE = 'Screen'
ON CONFLICT(MENU_ID) DO UPDATE SET USE_COUNT = USE_COUNT + 1, LAST_USED_AT = excluded.LAST_USED_AT
]]></statement>
  </query>
  <!-- 사용 통계 조회(관리) — 재배열 판단 근거: 잎별 전역 사용 횟수·최근 사용. -->
  <query id="SYS.MenuUsageStats" requiredPermission="sys:manage">
    <statement><![CDATA[
SELECT U.MENU_ID, M.MENU_NAME, M.UI_ID, U.USE_COUNT, U.LAST_USED_AT
FROM SYS_MENU_USAGE U
JOIN SYS_MENU M ON M.MENU_ID = U.MENU_ID
ORDER BY U.USE_COUNT DESC, U.LAST_USED_AT DESC
LIMIT 500
]]></statement>
  </query>
  <query id="SYS.TrimRecentMenus" kind="write" requiredPermission="sys:manage">
    <statement><![CDATA[
DELETE FROM SYS_RECENT_MENU
WHERE USER_ID = @currentUser
  AND MENU_ID NOT IN (
    SELECT MENU_ID FROM SYS_RECENT_MENU WHERE USER_ID = @currentUser ORDER BY LAST_USED_AT DESC LIMIT 10)
]]></statement>
  </query>

  <!-- 대시보드 요약(홈 KPI) — 모듈 횡단 카운트 1행. 상태 리터럴은 각 도메인 enum ToString() 저장값과 일치
       (WorkOrderStatus/ProductionPlanStatus/RecipeApprovalState/DeliveryOrderStatus). read 무해. -->
  <query id="SYS.DashboardSummary" access="public">
    <statement><![CDATA[
SELECT
  (SELECT COUNT(*) FROM EST_EQUIPMENT_ALARM WHERE CLEARED_AT IS NULL)                          AS ACTIVE_ALARMS,
  (SELECT COUNT(*) FROM EMS_WORK_ORDER WHERE STATUS IN ('Issued','InProgress'))                AS OPEN_WORK_ORDERS,
  (SELECT COUNT(*) FROM POM_PRODUCTION_PLAN WHERE STATUS IN ('Released','InProgress'))         AS ACTIVE_PLANS,
  (SELECT COUNT(*) FROM RMS_RECIPE WHERE APPROVAL_STATE IN ('WaitApproval','Approved1'))       AS PENDING_RECIPE_APPROVALS,
  (SELECT COUNT(*) FROM SHP_DELIVERY_ORDER WHERE STATUS IN ('Draft','Confirmed'))              AS OPEN_DELIVERY_ORDERS
]]></statement>
  </query>

  <!-- ===== 배치 작업 정의(V066, SYSTEM_2_BATCH_PROC_MANAGEMENT) — 1차=정의 CRUD, 실행 엔진 후속. ===== -->
  <query id="SYS.BatchProcessList" requiredPermission="sys:manage">
    <statement><![CDATA[
SELECT BATCH_ID, BATCH_NAME, BATCH_TYPE, BATCH_RULE, BATCH_OPTIONS, BATCH_INPUTDATA,
       AUTO_TRANSACTION, SAVE_HISTORY, DESCRIPTION, VALID_STATE, UPDATED_BY, UPDATED_AT
FROM SYS_BATCH_PROCESS
WHERE VALID_STATE = 'Valid'
ORDER BY BATCH_NAME, BATCH_ID
]]></statement>
  </query>
  <query id="SYS.UpsertBatchProcess" kind="write" requiredPermission="sys:manage">
    <statement><![CDATA[
INSERT INTO SYS_BATCH_PROCESS
    (BATCH_ID, BATCH_NAME, BATCH_TYPE, BATCH_RULE, BATCH_OPTIONS, BATCH_INPUTDATA,
     DESCRIPTION, VALID_STATE, CREATED_BY, CREATED_AT, UPDATED_BY, UPDATED_AT)
VALUES (@batchId, @batchName, @batchType, @batchRule, @batchOptions, @batchInputData,
        @description, 'Valid', @currentUser, @utcNow, @currentUser, @utcNow)
ON CONFLICT(BATCH_ID) DO UPDATE SET
    BATCH_NAME = excluded.BATCH_NAME, BATCH_TYPE = excluded.BATCH_TYPE, BATCH_RULE = excluded.BATCH_RULE,
    BATCH_OPTIONS = excluded.BATCH_OPTIONS, BATCH_INPUTDATA = excluded.BATCH_INPUTDATA,
    DESCRIPTION = excluded.DESCRIPTION, VALID_STATE = 'Valid',
    UPDATED_BY = excluded.UPDATED_BY, UPDATED_AT = excluded.UPDATED_AT
]]></statement>
  </query>
  <query id="SYS.DeleteBatchProcess" kind="write" requiredPermission="sys:manage">
    <statement><![CDATA[
UPDATE SYS_BATCH_PROCESS SET VALID_STATE = 'Invalid', UPDATED_BY = @currentUser, UPDATED_AT = @utcNow
WHERE BATCH_ID = @batchId
]]></statement>
  </query>
  <!-- 배치 실행 이력(V068) — 기록은 호스트 BatchProcessRunner가 수행(@currentUser=실행 주체 주입).
       requiredPermission은 원시 /command 잠금용(개인화 쿼리 규약과 동일). -->
  <query id="SYS.InsertBatchProcessHistory" kind="write" requiredPermission="sys:manage">
    <statement><![CDATA[
INSERT INTO SYS_BATCH_PROCESS_HISTORY
    (HISTORY_ID, BATCH_ID, STARTED_AT, FINISHED_AT, SUCCESS, AFFECTED, ERROR_MESSAGE, EXECUTED_BY)
VALUES (@historyId, @batchId, @startedAt, @finishedAt, @success, @affected, @errorMessage, @currentUser)
]]></statement>
  </query>
  <query id="SYS.BatchProcessHistoryList" requiredPermission="sys:manage">
    <statement><![CDATA[
SELECT HISTORY_ID, BATCH_ID, STARTED_AT, FINISHED_AT, SUCCESS, AFFECTED, ERROR_MESSAGE, EXECUTED_BY
FROM SYS_BATCH_PROCESS_HISTORY
WHERE (@batchId IS NULL OR BATCH_ID = @batchId)
ORDER BY STARTED_AT DESC
LIMIT 200
]]></statement>
  </query>
  <!-- 로그 보존 정리(V062/V064 주석의 '보존 정리는 후속' 이행) — 배치 엔진 실전 규약:
       BATCH_RULE=본 커맨드, BATCH_INPUTDATA={"retentionDays":N}. @utcNow는 실행기가 주입한다. -->
  <query id="SYS.PurgeOldAppLogs" kind="write" requiredPermission="sys:manage">
    <statement><![CDATA[
DELETE FROM SYS_APP_LOG WHERE LOGGED_AT < datetime(@utcNow, '-' || @retentionDays || ' days')
]]></statement>
  </query>
  <query id="SYS.PurgeOldRequestLogs" kind="write" requiredPermission="sys:manage">
    <statement><![CDATA[
DELETE FROM SYS_REQUEST_LOG WHERE REQUESTED_AT < datetime(@utcNow, '-' || @retentionDays || ' days')
]]></statement>
  </query>

  <!-- ===== 조건 저장/불러오기(§20.8) — @currentUser 스코프(§20.12와 동일 규약). '$latest'=마지막 조회 조건. -->
  <query id="SYS.ListConditionSettings" access="public">
    <statement><![CDATA[
SELECT NAME, SAVED_AT, VALUES_JSON
FROM SYS_CONDITION_SETTING
WHERE USER_ID = @currentUser AND MENU_ID = @menuId
ORDER BY SAVED_AT DESC
]]></statement>
  </query>
  <query id="SYS.UpsertConditionSetting" kind="write" requiredPermission="sys:manage">
    <statement><![CDATA[
INSERT INTO SYS_CONDITION_SETTING (USER_ID, MENU_ID, NAME, SAVED_AT, VALUES_JSON)
VALUES (@currentUser, @menuId, @name, @utcNow, @valuesJson)
ON CONFLICT(USER_ID, MENU_ID, NAME) DO UPDATE SET
    SAVED_AT = excluded.SAVED_AT, VALUES_JSON = excluded.VALUES_JSON
]]></statement>
  </query>
  <query id="SYS.DeleteConditionSetting" kind="write" requiredPermission="sys:manage">
    <statement><![CDATA[
DELETE FROM SYS_CONDITION_SETTING WHERE USER_ID = @currentUser AND MENU_ID = @menuId AND NAME = @name
]]></statement>
  </query>
  <!-- 사용자 저장 조건 한도(10) 초과분을 오래된 것부터 정리 — '$latest'는 한도 제외(§20.8). -->
  <query id="SYS.TrimConditionSettings" kind="write" requiredPermission="sys:manage">
    <statement><![CDATA[
DELETE FROM SYS_CONDITION_SETTING
WHERE USER_ID = @currentUser AND MENU_ID = @menuId AND NAME <> '$latest'
  AND NAME NOT IN (
    SELECT NAME FROM SYS_CONDITION_SETTING
    WHERE USER_ID = @currentUser AND MENU_ID = @menuId AND NAME <> '$latest'
    ORDER BY SAVED_AT DESC LIMIT 10)
]]></statement>
  </query>

  <query id="SYS.UpsertMenu" kind="write" requiredPermission="sys:manage">
    <statement><![CDATA[
INSERT INTO SYS_MENU (MENU_ID, MENU_NAME, PARENT_MENU_ID, DISPLAY_SEQUENCE, MENU_TYPE, UI_ID, VALID_STATE)
VALUES (@menuId, @menuName, @parentMenuId, @displaySequence, @menuType, @uiId, 'Valid')
ON CONFLICT(MENU_ID) DO UPDATE SET
    MENU_NAME = excluded.MENU_NAME, PARENT_MENU_ID = excluded.PARENT_MENU_ID,
    DISPLAY_SEQUENCE = excluded.DISPLAY_SEQUENCE, MENU_TYPE = excluded.MENU_TYPE,
    UI_ID = excluded.UI_ID, VALID_STATE = 'Valid'
]]></statement>
  </query>

  <query id="SYS.DeleteMenu" kind="write" requiredPermission="sys:manage">
    <statement><![CDATA[
UPDATE SYS_MENU SET VALID_STATE = 'Invalid' WHERE MENU_ID = @menuId
]]></statement>
  </query>

  <!-- ===== SmartUX 시스템관리 신규 마스터(V047) 조회 — 공지/메시지/다국어/Rule. read 무해(kind 미표기). ===== -->
  <query id="SYS.NoticeList" access="public">
    <statement><![CDATA[
SELECT NOTICE_ID, TITLE, NOTICE_TYPE, POSTED_BY, POSTED_AT, IS_ACTIVE FROM SYS_NOTICE ORDER BY POSTED_AT DESC
]]></statement>
  </query>
  <query id="SYS.MessageClassList" requiredPermission="sys:read">
    <statement><![CDATA[
SELECT MSG_CLASS_ID, MSG_CLASS_NAME, DESCRIPTION FROM SYS_MESSAGE_CLASS ORDER BY MSG_CLASS_NAME
]]></statement>
  </query>
  <query id="SYS.MessageList" requiredPermission="sys:read">
    <statement><![CDATA[
SELECT MSG_ID, MSG_CLASS_ID, MSG_CODE, MSG_TEXT, LANGUAGE FROM SYS_MESSAGE
WHERE (@msgClassId IS NULL OR MSG_CLASS_ID = @msgClassId)
ORDER BY MSG_CODE
]]></statement>
  </query>
  <query id="SYS.LanguageClassList" requiredPermission="sys:read">
    <statement><![CDATA[
SELECT LANG_CLASS_ID, LANG_CLASS_NAME, DESCRIPTION FROM SYS_LANGUAGE_CLASS ORDER BY LANG_CLASS_NAME
]]></statement>
  </query>
  <query id="SYS.I18nList" requiredPermission="sys:read">
    <statement><![CDATA[
SELECT I18N_ID, LANG_CLASS_ID, MESSAGE_KEY, LANGUAGE, TRANSLATION FROM SYS_I18N ORDER BY MESSAGE_KEY
]]></statement>
  </query>
  <query id="SYS.RuleList" requiredPermission="sys:manage">
    <statement><![CDATA[
SELECT RULE_ID, RULE_NAME, RULE_TYPE, IS_ACTIVE, DESCRIPTION FROM SYS_RULE ORDER BY RULE_NAME
]]></statement>
  </query>
  <!-- SYSTEM_2 파일 관리 — 배포 파일 메타(V012 SYS_DEPLOY_FILE) 재사용. -->
  <query id="SYS.DeployFileList" requiredPermission="sys:manage">
    <statement><![CDATA[
SELECT FILE_ID, VERSION, FILE_NAME, FILE_SIZE, DESCRIPTION, FORCE_UPDATE, IS_ACTIVE, UPLOADED_BY, UPLOADED_AT FROM SYS_DEPLOY_FILE ORDER BY UPLOADED_AT DESC
]]></statement>
  </query>
  <!-- SYSTEM2 요청 로그 뷰어 — API 요청 로그(V062). @method/@userId NULL-guard.
       서버측 페이징(P3-9 v2): @limit/@offset 미전달(직접 호출)이면 기존 최근 500 동작. -->
  <query id="SYS.RequestLogList" requiredPermission="sys:manage">
    <statement><![CDATA[
SELECT LOG_ID, METHOD, PATH, STATUS_CODE, ELAPSED_MS, USER_ID, CLIENT_IP, REQUESTED_AT
FROM SYS_REQUEST_LOG
WHERE (@method IS NULL OR METHOD = @method)
  AND (@userId IS NULL OR USER_ID = @userId)
ORDER BY REQUESTED_AT DESC
LIMIT COALESCE(@limit, 500) OFFSET COALESCE(@offset, 0)
]]></statement>
  </query>
  <!-- 요청 로그 총건수 — RequestLogList와 WHERE 일치(서버 페이저의 짝 count 쿼리, P3-9 v2). -->
  <query id="SYS.RequestLogListCount" requiredPermission="sys:manage">
    <statement><![CDATA[
SELECT COUNT(*) AS TOTAL
FROM SYS_REQUEST_LOG
WHERE (@method IS NULL OR METHOD = @method)
  AND (@userId IS NULL OR USER_ID = @userId)
]]></statement>
  </query>
  <!-- LOG_VIEWER — 앱 로그(V064). @logLevel NULL-guard. @limit/@offset 미전달이면 기존 최근 500. -->
  <query id="SYS.AppLogList" requiredPermission="sys:manage">
    <statement><![CDATA[
SELECT LOG_ID, LOG_LEVEL, CATEGORY, MESSAGE, EXCEPTION, LOGGED_AT
FROM SYS_APP_LOG
WHERE (@logLevel IS NULL OR LOG_LEVEL = @logLevel)
ORDER BY LOGGED_AT DESC
LIMIT COALESCE(@limit, 500) OFFSET COALESCE(@offset, 0)
]]></statement>
  </query>
  <!-- 앱 로그 총건수 — AppLogList와 WHERE 일치(서버 페이저의 짝 count 쿼리, P3-9 v2). -->
  <query id="SYS.AppLogListCount" requiredPermission="sys:manage">
    <statement><![CDATA[
SELECT COUNT(*) AS TOTAL
FROM SYS_APP_LOG
WHERE (@logLevel IS NULL OR LOG_LEVEL = @logLevel)
]]></statement>
  </query>

  <!-- 대시보드 시간대별 로그 추이(TrendChartWidget) — 시간 버킷별 로그 건수. 라벨=HH:00, 값=건수. -->
  <query id="SYS.AppLogHourlyTrend" requiredPermission="sys:manage">
    <statement><![CDATA[
SELECT strftime('%H:00', LOGGED_AT) AS HOUR_LABEL, COUNT(*) AS LOG_COUNT
FROM SYS_APP_LOG
GROUP BY strftime('%Y-%m-%d %H', LOGGED_AT)
ORDER BY strftime('%Y-%m-%d %H', LOGGED_AT) ASC
]]></statement>
  </query>
</queries>
