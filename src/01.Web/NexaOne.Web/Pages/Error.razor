@page "/Error"
@* 운영 예외 핸들러의 폴백 페이지 — Program.cs의 app.UseExceptionHandler("/Error")가 재실행하는 대상.
   서킷/서비스 상태가 불안정할 수 있어 의도적으로 주입·상호작용 없이 정적으로 렌더한다(홈 이동은 일반 링크). *@

<PageTitle>오류 - NexaOne MES</PageTitle>

<div class="page-header">
    <h1 class="page-title">오류가 발생했습니다</h1>
</div>

<div style="padding:1rem; display:flex; flex-direction:column; gap:0.75rem; max-width:42rem;">
    <p>요청을 처리하는 중 예기치 못한 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.</p>
    <p>문제가 계속되면 시스템 관리자에게 문의하세요.</p>
    <p><a href="/">홈으로 이동</a></p>
</div>
